<!DOCTYPE html>
<html>
  <head>
    <title>IdeasOnRailsApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-sm justify-content-between navbar-light bg-light">
    <ul class="nav ">
      <a href="/ideas" class="nav-link">Idea Factory</a>
    </ul>
    <ul class="nav">
  
      <a href="/ideas/new" class="nav-link">New Idea</a>
    </ul>
     <% if user_signed_in? %>
          Hello .<%= current_user.first_name %>
        
          <%= link_to "Sign out", session_path, method: :delete, data: {confirm: "Are you sure you want to logout?"} %>
        <% else %>
        <%=  link_to  'Sign In', new_session_path  %>
          
        <%=  link_to  'Sign Up', new_user_path  %>
        <% end %>
 
  <% if flash[:notice].present?%>
        <div class="alert alert-success aler-dismissible fade show" role="alert">
          <%= flash[:notice]%>
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">
            &times;
          </span>
          </button>
        </div>
    <% elsif flash[:alert].present?%>
      <div class="alert alert-danger aler-dismissible fade show" role="alert">
          <%= flash[:alert]%>
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">
            &times;
          </span>
          </button>
        </div>
    <% end %>
    
  </nav>
    <%= yield %>
  </body>
</html>
